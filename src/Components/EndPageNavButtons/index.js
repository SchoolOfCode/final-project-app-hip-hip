import React from "react";
import { Link } from "react-router";

const EndPageNavButtons = (
  <div>
    <Link to="/host/makeroom">
      <button>QUIT GAME</button>
    </Link>

    {/* 
    <Link to="/host " */}
  </div>
);

export default EndPageNavButtons;
